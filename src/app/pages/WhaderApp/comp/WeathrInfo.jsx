import React from 'react'
import PropTypes from 'prop-types'
let WeatherInfo= ({value})  => {
	return (<>
	
	</>)
}
WeatherInfo.propTypes = {
	value: PropTypes.number,
  }
  WeatherInfo.defaultProps = {
	value: 100
  }


export default WeatherInfo;
